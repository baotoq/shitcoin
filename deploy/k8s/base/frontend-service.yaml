apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    app: shitcoin
    component: frontend
spec:
  type: ClusterIP
  selector:
    app: shitcoin
    component: frontend
  ports:
    - port: 8080
      targetPort: 8080
