version: "2"

linters:
  default: standard
  enable:
    - govet
    - errcheck
    - staticcheck
    - unused
    - gosimple
    - ineffassign
    - misspell

formatters:
  enable:
    - gofmt

issues:
  exclude-dirs:
    - web
