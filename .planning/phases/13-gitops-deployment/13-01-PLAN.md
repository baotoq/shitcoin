---
phase: 13-gitops-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - argocd/application.yaml
autonomous: true
requirements:
  - GIT-01
  - GIT-02

must_haves:
  truths:
    - "ArgoCD Application CR defines auto-sync with prune and selfHeal targeting the Kustomize dev overlay"
    - "ArgoCD Application CR is stored in argocd/ directory, completely separate from deploy/k8s/"
  artifacts:
    - path: "argocd/application.yaml"
      provides: "ArgoCD Application custom resource"
      contains: "kind: Application"
  key_links:
    - from: "argocd/application.yaml"
      to: "deploy/k8s/overlays/dev"
      via: "spec.source.path"
      pattern: "path: deploy/k8s/overlays/dev"
---

<objective>
Create the ArgoCD Application custom resource that completes the GitOps deployment loop.

Purpose: This is the final phase of the v1.1 milestone. The Application CR tells ArgoCD to watch the Kustomize dev overlay in git and automatically sync cluster state, enabling push-to-deploy GitOps workflow.
Output: `argocd/application.yaml` -- a single YAML file defining the ArgoCD Application CR.
</objective>

<execution_context>
@/Users/baotoq/.claude/get-shit-done/workflows/execute-plan.md
@/Users/baotoq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-gitops-deployment/13-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ArgoCD Application CR</name>
  <files>argocd/application.yaml</files>
  <action>
Create `argocd/` directory and `argocd/application.yaml` containing the ArgoCD Application custom resource with these exact specifications:

- apiVersion: argoproj.io/v1alpha1
- kind: Application
- metadata.name: shitcoin
- metadata.namespace: argocd
- metadata.finalizers: [resources-finalizer.argocd.argoproj.io] (cleans up managed resources on Application deletion)
- spec.project: default
- spec.source.repoURL: https://github.com/baotoq/shitcoin.git
- spec.source.targetRevision: HEAD
- spec.source.path: deploy/k8s/overlays/dev
- spec.destination.server: https://kubernetes.default.svc
- spec.destination.namespace: default
- spec.syncPolicy.automated.prune: true (removes orphaned resources)
- spec.syncPolicy.automated.selfHeal: true (reverts manual cluster drift)
- spec.syncPolicy.syncOptions: [CreateNamespace=true]

Critical placement requirement (GIT-02): The file MUST be in `argocd/` which is completely separate from `deploy/k8s/` to prevent ArgoCD from recursively managing its own Application CR.
  </action>
  <verify>
    <automated>test -f argocd/application.yaml && grep -q 'kind: Application' argocd/application.yaml && grep -q 'deploy/k8s/overlays/dev' argocd/application.yaml && grep -q 'automated' argocd/application.yaml && grep -q 'prune: true' argocd/application.yaml && grep -q 'selfHeal: true' argocd/application.yaml && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
argocd/application.yaml exists with: correct apiVersion (argoproj.io/v1alpha1), kind (Application), source.path pointing to deploy/k8s/overlays/dev, automated sync policy with prune and selfHeal enabled, and the file lives in argocd/ not inside deploy/k8s/.
  </done>
</task>

</tasks>

<verification>
- `test -f argocd/application.yaml` -- file exists in correct location
- `grep -q 'deploy/k8s/overlays/dev' argocd/application.yaml` -- points to correct Kustomize overlay
- `grep -q 'automated' argocd/application.yaml` -- auto-sync configured
- `! echo argocd/ | grep -q deploy/k8s` -- argocd/ is outside the watched path
</verification>

<success_criteria>
1. argocd/application.yaml contains a valid ArgoCD Application CR with auto-sync (prune + selfHeal) pointing to deploy/k8s/overlays/dev
2. The file is stored in argocd/ directory, completely separate from the deploy/k8s/ directory that ArgoCD watches
</success_criteria>

<output>
After completion, create `.planning/phases/13-gitops-deployment/13-01-SUMMARY.md`
</output>
