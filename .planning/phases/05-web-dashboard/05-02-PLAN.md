---
phase: 05-web-dashboard
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - internal/handler/api/block_handler.go
  - internal/handler/api/status_handler.go
  - internal/handler/api/mempool_handler.go
  - internal/handler/api/search_handler.go
  - internal/handler/api/address_handler.go
  - internal/handler/api/tx_handler.go
  - internal/handler/api/routes.go
  - internal/handler/api/block_handler_test.go
  - internal/handler/api/status_handler_test.go
autonomous: true
requirements: [DASH-01, DASH-02, DASH-04, DASH-05]

must_haves:
  truths:
    - "GET /api/blocks returns paginated block list newest-first"
    - "GET /api/blocks/:height returns full block with transactions"
    - "GET /api/blocks/hash/:hash returns block by hash"
    - "GET /api/status returns chain height, mempool size, peer count, mining status"
    - "GET /api/mempool returns pending transactions"
    - "GET /api/tx/:hash returns transaction with block context"
    - "GET /api/address/:addr returns balance and UTXOs"
    - "GET /api/search?q=... resolves block hash, tx hash, or address"
  artifacts:
    - path: "internal/handler/api/block_handler.go"
      provides: "Block explorer REST handlers"
      exports: ["BlocksHandler", "BlockByHeightHandler", "BlockByHashHandler"]
    - path: "internal/handler/api/status_handler.go"
      provides: "Node status endpoint"
      exports: ["StatusHandler"]
    - path: "internal/handler/api/search_handler.go"
      provides: "Universal search endpoint"
      exports: ["SearchHandler"]
    - path: "internal/handler/api/routes.go"
      provides: "Route registration with real handlers"
      exports: ["RegisterRoutes"]
  key_links:
    - from: "internal/handler/api/block_handler.go"
      to: "internal/svc/service_context.go"
      via: "svcCtx.ChainRepo.GetBlock/GetBlockByHeight/GetBlocksInRange"
      pattern: "svcCtx\\.ChainRepo"
    - from: "internal/handler/api/status_handler.go"
      to: "internal/svc/service_context.go"
      via: "svcCtx.Chain.Height(), svcCtx.Mempool.Count(), peerCounter.PeerCount()"
      pattern: "svcCtx\\.Chain\\.Height"
    - from: "internal/handler/api/search_handler.go"
      to: "internal/handler/api/block_handler.go"
      via: "Reuses block/tx lookup logic"
      pattern: "SearchHandler"
---

<objective>
Implement all REST API handlers for the block explorer, node status, mempool, address lookup, transaction lookup, and search.

Purpose: These endpoints provide the data layer that the React frontend will consume for all non-real-time features (block browsing, search, address lookup, initial page loads).

Output: 6 handler files with full implementations, updated routes.go wiring real handlers, and tests for key handlers.
</objective>

<execution_context>
@/Users/baotoq/.claude/get-shit-done/workflows/execute-plan.md
@/Users/baotoq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-web-dashboard/05-CONTEXT.md
@.planning/phases/05-web-dashboard/05-RESEARCH.md
@.planning/phases/05-web-dashboard/05-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From internal/handler/api/types.go:
```go
type StatusResponse struct {
    ChainHeight    uint64 `json:"chain_height"`
    LatestBlockHash string `json:"latest_block_hash"`
    MempoolSize    int    `json:"mempool_size"`
    PeerCount      int    `json:"peer_count"`
    IsMining       bool   `json:"is_mining"`
}
type BlockListResponse struct { ... }
type AddressResponse struct { ... }
type SearchResult struct { ... }
type ErrorResponse struct { ... }
```

From internal/handler/api/routes.go:
```go
type PeerCounter interface { PeerCount() int }
func RegisterRoutes(server *rest.Server, svcCtx *svc.ServiceContext, peerCounter PeerCounter, hub ...)
```

From internal/domain/chain/repository.go:
```go
type Repository interface {
    GetBlock(ctx context.Context, hash block.Hash) (*block.Block, error)
    GetBlockByHeight(ctx context.Context, height uint64) (*block.Block, error)
    GetBlocksInRange(ctx context.Context, startHeight, endHeight uint64) ([]*block.Block, error)
    GetChainHeight(ctx context.Context) (uint64, error)
}
```

From internal/infrastructure/persistence/bbolt/storage_model.go:
```go
func BlockModelFromDomain(b *block.Block) *BlockModel
func TxModelFromDomain(t *tx.Transaction) TxModel
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement block explorer, status, and mempool handlers</name>
  <files>internal/handler/api/block_handler.go, internal/handler/api/status_handler.go, internal/handler/api/mempool_handler.go, internal/handler/api/tx_handler.go, internal/handler/api/block_handler_test.go, internal/handler/api/status_handler_test.go</files>
  <behavior>
    - Test 1: BlocksHandler returns paginated blocks newest-first with correct page/limit/total
    - Test 2: BlockByHeightHandler returns full block with transactions for valid height
    - Test 3: BlockByHeightHandler returns 404 for non-existent height
    - Test 4: StatusHandler returns correct chain height, mempool size, peer count
    - Test 5: MempoolHandler returns all pending transactions as TxModel array
  </behavior>
  <action>
    Create `internal/handler/api/block_handler.go`:
    - `BlocksHandler(svcCtx)` -- GET /api/blocks?page=1&limit=20: parse query params, compute start/end height from chain height for reverse ordering, call ChainRepo.GetBlocksInRange, convert with BlockModelFromDomain, return BlockListResponse. Default page=1, limit=20, cap limit at 100.
    - `BlockByHeightHandler(svcCtx)` -- GET /api/blocks/:height: parse path param, call ChainRepo.GetBlockByHeight, convert with BlockModelFromDomain, return BlockModel. Return 404 JSON error if not found.
    - `BlockByHashHandler(svcCtx)` -- GET /api/blocks/hash/:hash: parse path param, parse hex hash via block.HashFromHex, call ChainRepo.GetBlock, convert, return BlockModel. Return 404 if not found.

    Create `internal/handler/api/tx_handler.go`:
    - `TxHandler(svcCtx)` -- GET /api/tx/:hash: scan blocks from tip backwards looking for matching tx hash. For each block, iterate RawTransactions(), type-assert to *tx.Transaction, compare ID().String() with param. Return TxModel + block height/hash context. Return 404 if not found. This is O(n) scan -- acceptable for educational project per research.

    Create `internal/handler/api/status_handler.go`:
    - `StatusHandler(svcCtx, peerCounter)` -- GET /api/status: return StatusResponse with Chain.Height(), Chain.LatestBlock().Hash().String(), Mempool.Count(), peerCounter.PeerCount(), IsMining (from atomic bool or similar).
    - peerCounter can be nil (returns 0 if nil -- handles non-P2P mode).

    Create `internal/handler/api/mempool_handler.go`:
    - `MempoolHandler(svcCtx)` -- GET /api/mempool: call Mempool.Transactions(), convert each with TxModelFromDomain, return array.

    All handlers use `httpx.OkJsonCtx` for success and `httpx.ErrorCtx` or manual JSON error for failures.
    Use `go-zero/rest/httpx` for response writing (consistent with go-zero patterns).

    Create test files using httptest.NewRequest + httptest.NewRecorder pattern with mock ServiceContext. Use testify/assert.
  </action>
  <verify>
    <automated>cd /Users/baotoq/Work/shitcoin && go test ./internal/handler/api/... -v -count=1</automated>
  </verify>
  <done>Block explorer endpoints return correct data. Status returns live node metrics. Mempool returns pending txs. Tx lookup scans chain. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Implement search and address handlers, wire all routes</name>
  <files>internal/handler/api/search_handler.go, internal/handler/api/address_handler.go, internal/handler/api/routes.go</files>
  <action>
    Create `internal/handler/api/search_handler.go`:
    - `SearchHandler(svcCtx)` -- GET /api/search?q=...: detect query type by length and format:
      - 64 hex chars: try as block hash first (ChainRepo.GetBlock), then scan as tx hash
      - Valid Base58Check address (starts with 1 or 3, 25-34 chars): treat as address
      - Numeric string: treat as block height
    - Return SearchResult with type ("block", "tx", "address", "height") and relevant fields
    - Return 404 with ErrorResponse if nothing found

    Create `internal/handler/api/address_handler.go`:
    - `AddressHandler(svcCtx)` -- GET /api/address/:addr: call UTXOSet.GetByAddress(addr), sum values for balance, convert UTXOs to UTXOModel array, return AddressResponse.
    - Return empty UTXOs array and 0 balance for unknown addresses (not 404).

    Update `internal/handler/api/routes.go`:
    - Replace all stub handlers with real handler functions
    - Wire: BlocksHandler, BlockByHeightHandler, BlockByHashHandler, TxHandler, StatusHandler, MempoolHandler, SearchHandler, AddressHandler
    - Keep PeerCounter interface and WebSocket hub handler parameter
    - Ensure all routes use `http.MethodGet`
  </action>
  <verify>
    <automated>cd /Users/baotoq/Work/shitcoin && go build ./internal/handler/api/... && go test ./internal/handler/api/... -v -count=1</automated>
  </verify>
  <done>Search detects hash/address/height and returns correct results. Address endpoint returns balance and UTXOs. All routes wired to real handlers. Full handler suite compiles and tests pass.</done>
</task>

</tasks>

<verification>
- `go build ./...` succeeds
- `go test ./internal/handler/api/... -count=1` passes
- `go vet ./internal/handler/api/...` passes
- All 8 REST endpoints return correct data format
</verification>

<success_criteria>
- Block explorer: browse blocks paginated, view block by height/hash, view transactions
- Status endpoint: chain height, mempool size, peer count
- Mempool endpoint: list pending transactions
- Search: resolve block hash, tx hash, address, or block height
- Address: balance and UTXO list
- All handlers tested with mock data
</success_criteria>

<output>
After completion, create `.planning/phases/05-web-dashboard/05-02-SUMMARY.md`
</output>
