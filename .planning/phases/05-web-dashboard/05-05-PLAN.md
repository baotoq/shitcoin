---
phase: 05-web-dashboard
plan: 05
type: execute
wave: 4
depends_on: [05-04]
files_modified:
  - web/src/pages/BlockExplorer.tsx
  - web/src/pages/BlockDetail.tsx
  - web/src/pages/TxDetail.tsx
  - web/src/pages/Mempool.tsx
  - web/src/pages/Mining.tsx
  - web/src/pages/Address.tsx
  - web/src/components/BlockCard.tsx
  - web/src/components/TxTable.tsx
  - web/src/components/MiningVisualizer.tsx
  - web/src/App.tsx
autonomous: false

requirements: [DASH-01, DASH-02, DASH-03, DASH-04, DASH-05]

must_haves:
  truths:
    - "User can browse paginated blocks in block explorer page"
    - "User can view a single block with all its transactions"
    - "User can view transaction detail with inputs and outputs"
    - "User can see pending mempool transactions updated live"
    - "User can watch mining in real-time with nonce, hash, and target visualization"
    - "User can view address balance and UTXO list"
    - "All pages navigate correctly via sidebar and internal links"
  artifacts:
    - path: "web/src/pages/BlockExplorer.tsx"
      provides: "Paginated block list page"
      contains: "BlockExplorer"
    - path: "web/src/pages/BlockDetail.tsx"
      provides: "Single block view with transactions"
      contains: "BlockDetail"
    - path: "web/src/pages/TxDetail.tsx"
      provides: "Transaction detail with inputs/outputs"
      contains: "TxDetail"
    - path: "web/src/pages/Mining.tsx"
      provides: "Real-time mining visualization page"
      contains: "MiningVisualizer"
    - path: "web/src/pages/Mempool.tsx"
      provides: "Live mempool transaction list"
      contains: "Mempool"
    - path: "web/src/pages/Address.tsx"
      provides: "Address balance and UTXO list"
      contains: "Address"
    - path: "web/src/components/BlockCard.tsx"
      provides: "Compact block summary card component"
      exports: ["BlockCard"]
    - path: "web/src/components/TxTable.tsx"
      provides: "Transaction list table component"
      exports: ["TxTable"]
    - path: "web/src/components/MiningVisualizer.tsx"
      provides: "Real-time mining nonce/hash/target display"
      exports: ["MiningVisualizer"]
  key_links:
    - from: "web/src/pages/BlockExplorer.tsx"
      to: "web/src/lib/api.ts"
      via: "fetchBlocks for paginated block list"
      pattern: "fetchBlocks"
    - from: "web/src/pages/Mining.tsx"
      to: "web/src/hooks/useWebSocket.ts"
      via: "Listens for mining_progress, mining_started, mining_stopped events"
      pattern: "mining_progress"
    - from: "web/src/pages/Mempool.tsx"
      to: "web/src/hooks/useWebSocket.ts"
      via: "Listens for mempool_changed events to refresh"
      pattern: "mempool_changed"
    - from: "web/src/App.tsx"
      to: "web/src/pages/*.tsx"
      via: "React Router route definitions"
      pattern: "Route.*path"
---

<objective>
Build all remaining frontend pages: Block Explorer, Block Detail, Transaction Detail, Mempool, Mining Visualizer, and Address -- replacing the placeholder routes from Plan 04.

Purpose: Completes the full web dashboard UI, delivering all 5 DASH requirements with a browser-rendered interface. Users can browse blocks, inspect transactions, monitor mempool, watch mining in real-time, and look up addresses.

Output: 6 page components, 3 reusable components (BlockCard, TxTable, MiningVisualizer), all routes wired.
</objective>

<execution_context>
@/Users/baotoq/.claude/get-shit-done/workflows/execute-plan.md
@/Users/baotoq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-web-dashboard/05-CONTEXT.md
@.planning/phases/05-web-dashboard/05-RESEARCH.md
@.planning/phases/05-web-dashboard/05-04-SUMMARY.md

<interfaces>
<!-- From Plan 04 outputs -->

From web/src/types/api.ts:
```typescript
interface StatusResponse {
  chain_height: number;
  latest_block_hash: string;
  mempool_size: number;
  peer_count: number;
  is_mining: boolean;
}
interface BlockListResponse {
  blocks: BlockModel[];
  total: number;
  page: number;
  limit: number;
}
interface BlockModel {
  hash: string;
  header: HeaderModel;
  height: number;
  message?: string;
  transactions: TxModel[];
}
interface HeaderModel {
  version: number;
  prev_block_hash: string;
  merkle_root: string;
  timestamp: number;
  bits: number;
  nonce: number;
}
interface TxModel {
  id: string;
  inputs: TxInputModel[];
  outputs: TxOutputModel[];
}
interface TxInputModel {
  txid: string;
  vout: number;
  signature: string;
  pubkey: string;
}
interface TxOutputModel {
  value: number;
  pubkey_hash: string;
  address: string;
}
interface AddressResponse {
  address: string;
  balance: number;
  utxos: UTXOModel[];
}
interface MiningProgressPayload {
  nonce: number;
  hash: string;
  target: string;
  difficulty: number;
  block_height: number;
}
interface WSMessage {
  type: string;
  payload: unknown;
}
```

From web/src/lib/api.ts:
```typescript
fetchStatus(): Promise<StatusResponse>
fetchBlocks(page?, limit?): Promise<BlockListResponse>
fetchBlockByHeight(height): Promise<BlockModel>
fetchBlockByHash(hash): Promise<BlockModel>
fetchTx(hash): Promise<TxModel>
fetchMempool(): Promise<TxModel[]>
fetchAddress(addr): Promise<AddressResponse>
searchQuery(q): Promise<SearchResult>
```

From web/src/hooks/useWebSocket.ts:
```typescript
useWebSocket(url?): { lastMessage: WSMessage | null, isConnected: boolean }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build BlockCard, TxTable, MiningVisualizer components and Block Explorer, Block Detail, Tx Detail pages</name>
  <files>web/src/components/BlockCard.tsx, web/src/components/TxTable.tsx, web/src/components/MiningVisualizer.tsx, web/src/pages/BlockExplorer.tsx, web/src/pages/BlockDetail.tsx, web/src/pages/TxDetail.tsx</files>
  <action>
    Create reusable components and block/transaction pages:

    1. Create `web/src/components/BlockCard.tsx`:
       - Props: `block: BlockModel`, `onClick?: () => void`
       - Renders shadcn Card with: height (large, bold), hash (truncated first 16 chars + "..."), transaction count, relative time from timestamp (e.g., "2 min ago" using simple `Date.now() - timestamp*1000` calculation)
       - Click navigates to `/blocks/{height}`
       - Compact horizontal layout suitable for list display
       - Dark styling: bg-zinc-900, border-zinc-800, hover:bg-zinc-800 transition

    2. Create `web/src/components/TxTable.tsx`:
       - Props: `transactions: TxModel[]`, `showBlockContext?: boolean`
       - Renders shadcn Table with columns:
         - TX ID (truncated first 16 chars, links to `/tx/{id}`)
         - Inputs count
         - Outputs count
         - Total output value (sum of output values, formatted as satoshis / 100000000 for display in coins)
       - If transaction has single input with txid of all zeros: show "Coinbase" badge instead of normal input count
       - Empty state: "No transactions" message
       - Dark styling consistent with theme

    3. Create `web/src/components/MiningVisualizer.tsx`:
       - Props: `progress: MiningProgressPayload | null`, `isActive: boolean`, `blockHeight?: number`
       - When isActive=false: shows "Mining is idle" with gray text
       - When isActive=true and progress exists:
         - Block Height being mined (large)
         - Current Nonce (formatted with commas, e.g., "1,234,567")
         - Current Hash: full 64-char hex displayed in monospace font (text-xs), with leading zeros highlighted in green (chars that match target)
         - Target: full 64-char hex in monospace (text-xs), displayed below hash for visual comparison
         - Difficulty: numeric value
         - Animated pulse dot next to "Mining..." label
       - Use monospace font (font-mono) for all hash/nonce displays
       - The hash vs target comparison is the key educational visualization

    4. Create `web/src/pages/BlockExplorer.tsx`:
       - Route: `/blocks`
       - Fetches paginated blocks via `fetchBlocks(page, 20)` on mount and page change
       - Displays blocks as a list of BlockCard components
       - Pagination controls at bottom: Previous / Next buttons (shadcn Button), showing "Page X of Y"
       - Page state stored in URL search params (`?page=2`) via useSearchParams
       - On `new_block` WebSocket event: if on page 1, prepend new block to list (or refetch)
       - Header: "Block Explorer" with total block count from response

    5. Create `web/src/pages/BlockDetail.tsx`:
       - Route: `/blocks/:height`
       - Fetches block via `fetchBlockByHeight(height)` using useParams
       - Displays:
         - Block header info in a detail card: height, hash (full, monospace), prev_block_hash (links to prev block), merkle_root, timestamp (formatted date), bits (difficulty), nonce
         - Message field if present (genesis block)
         - Transaction list using TxTable component
         - Navigation: "Previous Block" / "Next Block" links (if applicable)
       - Loading state with skeleton
       - 404 handling: "Block not found" message

    6. Create `web/src/pages/TxDetail.tsx`:
       - Route: `/tx/:hash`
       - Fetches transaction via `fetchTx(hash)` using useParams
       - Displays:
         - TX ID (full hash, monospace)
         - Inputs section: list each input with txid (links to source tx unless coinbase), vout index, pubkey (truncated)
         - Outputs section: list each output with value (in coins), address (links to `/address/{addr}`)
         - Coinbase indicator if applicable
       - Loading state, 404 handling
  </action>
  <verify>
    <automated>cd /Users/baotoq/Work/shitcoin/web && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>BlockCard, TxTable, MiningVisualizer components created. Block Explorer shows paginated blocks. Block Detail shows full block with transactions. Tx Detail shows inputs/outputs. All compile successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Build Mempool, Mining, Address pages and wire all routes in App.tsx</name>
  <files>web/src/pages/Mempool.tsx, web/src/pages/Mining.tsx, web/src/pages/Address.tsx, web/src/App.tsx</files>
  <action>
    Create remaining pages and complete routing:

    1. Create `web/src/pages/Mempool.tsx`:
       - Route: `/mempool`
       - Fetches pending transactions via `fetchMempool()` on mount
       - Displays using TxTable component
       - Listens to WebSocket `mempool_changed` events: on change, refetch mempool
       - Also listens for `new_tx` events: could append to list or refetch
       - Header: "Mempool" with transaction count badge
       - Empty state: "No pending transactions" with descriptive text
       - Auto-refreshes on new block (mempool drains after mining)

    2. Create `web/src/pages/Mining.tsx`:
       - Route: `/mining`
       - Uses `useWebSocket()` hook to listen for mining events
       - State management:
         - `isActive: boolean` -- set true on `mining_started`, false on `mining_stopped`
         - `progress: MiningProgressPayload | null` -- updated on each `mining_progress` event
         - `blockHeight: number` -- from `mining_started` payload
         - `lastMinedBlock: BlockModel | null` -- fetched on `mining_stopped` (fetch the block that was just mined)
       - Renders MiningVisualizer component with current state
       - Below visualizer: "Last Mined Block" section showing the most recently mined block (if available) using BlockCard
       - Also fetch initial mining status from `fetchStatus()` to set isActive on page load
       - Educational note: brief explanation text "Watch the miner search for a nonce that produces a hash below the target value"

    3. Create `web/src/pages/Address.tsx`:
       - Route: `/address/:addr`
       - Fetches address data via `fetchAddress(addr)` using useParams
       - Displays:
         - Address (full, monospace, in header)
         - Balance (formatted as coins from satoshis, large text)
         - UTXO list in a table: TX ID (truncated, links to `/tx/{txid}`), Output Index, Value (in coins)
       - Empty state: "No UTXOs found for this address"
       - Loading state with skeleton

    4. Update `web/src/App.tsx`:
       - Replace ALL placeholder route elements with actual page components:
         - index: `<Dashboard />`
         - `/blocks`: `<BlockExplorer />`
         - `/blocks/:height`: `<BlockDetail />`
         - `/tx/:hash`: `<TxDetail />`
         - `/mempool`: `<Mempool />`
         - `/mining`: `<Mining />`
         - `/address/:addr`: `<Address />`
       - Add imports for all page components
       - Add a catch-all route for 404: "Page not found" with link back to Dashboard
  </action>
  <verify>
    <automated>cd /Users/baotoq/Work/shitcoin/web && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Mempool page shows live pending transactions. Mining page visualizes real-time nonce/hash/target. Address page shows balance and UTXOs. All routes wired with no placeholders remaining. Production build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete web dashboard end-to-end</name>
  <files>web/src/App.tsx</files>
  <action>Human verifies the complete React SPA web dashboard with 7 pages (Dashboard, Block Explorer, Block Detail, Transaction Detail, Mempool, Mining Visualizer, Address), StatusBar, SearchBar, sidebar navigation, WebSocket live updates, and REST API integration.</action>
  <what-built>Complete React SPA web dashboard with all pages, components, live WebSocket updates, and REST API integration.</what-built>
  <how-to-verify>
    1. Start the Go backend: `go run cmd/shitcoin/main.go -f etc/shitcoin.yaml startnode -mine <YOUR_ADDRESS>`
    2. In a separate terminal: `cd web && npm run dev`
    3. Open http://localhost:5173/ in browser
    4. Verify Dashboard: shows chain height, peer count (0 for single node), mempool size, mining status
    5. Click "Blocks" in sidebar: verify paginated block list loads
    6. Click a block: verify block detail page with transactions
    7. Click a transaction ID: verify tx detail with inputs/outputs
    8. Click "Mempool" in sidebar: verify pending transactions (or empty state)
    9. Click "Mining" in sidebar: verify real-time mining visualization (nonce counting, hash/target comparison)
    10. Use SearchBar: search for a block hash, then a block height, then an address
    11. Click an address in a transaction output: verify address page shows balance and UTXOs
    12. Verify StatusBar updates live as new blocks are mined
  </how-to-verify>
  <verify>Human visual and functional verification of all dashboard pages and live updates</verify>
  <done>All 7 pages render correctly, StatusBar shows live metrics, SearchBar navigates to results, Mining page shows real-time visualization, WebSocket updates work across all pages.</done>
  <resume-signal>Type "approved" or describe visual/functional issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `cd web && npm run build` succeeds with zero TypeScript errors
- All 7 routes render their page components (no placeholders)
- WebSocket events update Mining page and Mempool page in real-time
- Block explorer pagination works with URL params
- Internal links between blocks, transactions, and addresses all navigate correctly
- SearchBar resolves block hashes, tx hashes, heights, and addresses
</verification>

<success_criteria>
- DASH-01: Block Explorer page with paginated blocks, Block Detail with transactions, links between blocks/txs
- DASH-02: StatusBar shows live metrics, Dashboard has 4 stat cards, all updated via WebSocket
- DASH-03: Mining page with MiningVisualizer showing real-time nonce, hash vs target comparison
- DASH-04: Mempool page showing pending transactions, updated live via WebSocket
- DASH-05: SearchBar resolves block hash, tx hash, height, and address with navigation to results
- All pages use dark theme with consistent shadcn/ui components
- Human verification confirms full dashboard works end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/05-web-dashboard/05-05-SUMMARY.md`
</output>
