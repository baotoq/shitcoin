---
phase: 04.1-use-test-assert
plan: 02
type: execute
wave: 2
depends_on: ["04.1-01"]
files_modified:
  - internal/infrastructure/persistence/bbolt/chain_repo_test.go
  - internal/infrastructure/persistence/bbolt/utxo_repo_test.go
  - internal/domain/p2p/server_test.go
  - internal/domain/p2p/relay_test.go
  - internal/domain/p2p/sync_test.go
  - internal/domain/p2p/reorg_test.go
  - internal/domain/chain/chain_test.go
autonomous: true
requirements:
  - TEST-MIGRATE-02

must_haves:
  truths:
    - "All 7 complex test files use testify assert/require instead of raw t.Errorf/t.Fatalf"
    - "bbolt repo tests use testify/suite with SetupTest for fresh DB per test"
    - "P2P server/relay/sync/reorg tests use testify/suite with shared setup helpers"
    - "server_test.go mockChainRepo and chain_test.go mockMempoolAdder converted to testify/mock"
    - "Hand-rolled fake implementations (fullMockChainRepo, mockUTXORepo, reorgMockChainRepo) kept as-is but use assert/require for their test assertions"
    - "All existing tests pass after migration"
    - "No mixed assertion styles remain in any test file"
  artifacts:
    - path: "internal/infrastructure/persistence/bbolt/chain_repo_test.go"
      provides: "Suite-based bbolt chain repo tests"
      contains: "suite.Suite"
    - path: "internal/infrastructure/persistence/bbolt/utxo_repo_test.go"
      provides: "Suite-based bbolt UTXO repo tests"
      contains: "suite.Suite"
    - path: "internal/domain/p2p/server_test.go"
      provides: "P2P server tests with testify/mock"
      contains: "mock.Mock"
    - path: "internal/domain/chain/chain_test.go"
      provides: "Chain tests with mockMempoolAdder as testify/mock"
      contains: "mock.Mock"
  key_links:
    - from: "bbolt/*_repo_test.go"
      to: "testify/suite"
      via: "suite.Suite embedding and suite.Run driver"
      pattern: "suite\\.Run\\(t,"
    - from: "p2p/server_test.go"
      to: "testify/mock"
      via: "mock.Mock embedding in mockChainRepo"
      pattern: "mock\\.Mock"
---

<objective>
Migrate the 7 complex test files that require testify/suite for shared setup/teardown and testify/mock for stub mocks. This includes bbolt repository tests, P2P integration tests, and chain aggregate tests.

Purpose: Complete the full codebase migration to testify. These files need structural refactoring beyond simple assertion replacement: suite wrapping for bbolt/P2P tests, and mock conversion for simple stub mocks.
Output: 7 test files fully migrated with suites and mocks, all tests green, zero remaining stdlib assertions in codebase.
</objective>

<execution_context>
@/Users/baotoq/.claude/get-shit-done/workflows/execute-plan.md
@/Users/baotoq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.1-use-test-assert/04.1-CONTEXT.md
@.planning/phases/04.1-use-test-assert/04.1-RESEARCH.md
@.planning/phases/04.1-use-test-assert/04.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate bbolt repo tests to testify/suite and chain_test.go with mock conversion</name>
  <files>internal/infrastructure/persistence/bbolt/chain_repo_test.go, internal/infrastructure/persistence/bbolt/utxo_repo_test.go, internal/domain/chain/chain_test.go</files>
  <action>
1. **bbolt/chain_repo_test.go** (366 lines) -- Convert to testify/suite:
   - Create `ChainRepoSuite struct { suite.Suite; db *bolt.DB; repo *BboltChainRepository }` (use actual type names from codebase).
   - Implement `SetupTest()`: create fresh DB via `bolt.Open` in `s.T().TempDir()`, call `NewBbolt...Repository(db)`, register `s.T().Cleanup(func() { db.Close() })`.
   - Convert each `TestXxx` function to `(s *ChainRepoSuite) TestXxx()` method.
   - Replace all assertions with `s.Assert().Equal(...)`, `s.Require().NoError(...)` etc.
   - Add driver: `func TestChainRepoSuite(t *testing.T) { suite.Run(t, new(ChainRepoSuite)) }`.
   - Move shared test helpers (like `createTestBlock`) into suite methods.

2. **bbolt/utxo_repo_test.go** (296 lines) -- Convert to testify/suite:
   - Same pattern as chain_repo: `UTXORepoSuite struct { suite.Suite; db *bolt.DB; repo *BboltUTXORepository }`.
   - `SetupTest()` creates fresh DB + repo per test.
   - Convert all test functions to suite methods.
   - Replace assertions with suite assert/require.

3. **chain/chain_test.go** (470 lines) -- Mixed migration:
   - **mockMempoolAdder**: Convert to testify/mock. It's a simple 1-method interface mock. Create: `type MockMempoolAdder struct { mock.Mock }` with the AddTransaction method calling `m.Called(...)`. Use `mockObj.On("AddTransaction", ...).Return(...)` in tests, and `mockObj.AssertExpectations(t)` at end.
   - **mockChainRepo and mockUTXORepo**: KEEP hand-rolled -- these are fake in-memory stores with real state tracking logic (maps, mutexes). Only convert their test ASSERTIONS to assert/require.
   - Convert all `t.Errorf`/`t.Fatalf` to assert/require throughout the file.
   - Use `require` for setup (repo creation, block saving), `assert` for value checks.

4. Run tests: `go test ./internal/infrastructure/persistence/bbolt/... ./internal/domain/chain/...`
  </action>
  <verify>
    <automated>cd /Users/baotoq/Work/shitcoin && go test ./internal/infrastructure/persistence/bbolt/... ./internal/domain/chain/...</automated>
  </verify>
  <done>bbolt tests run as suites with fresh DB per test, chain_test mockMempoolAdder uses testify/mock, all hand-rolled fakes preserved but use assert/require, all tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate P2P test files to testify/suite and convert server_test mock</name>
  <files>internal/domain/p2p/server_test.go, internal/domain/p2p/relay_test.go, internal/domain/p2p/sync_test.go, internal/domain/p2p/reorg_test.go</files>
  <action>
1. **p2p/server_test.go** (172 lines) -- Suite + mock conversion:
   - **mockChainRepo**: Convert to testify/mock. It's a simple stub mock (returns predefined values). Create `type MockChainRepo struct { mock.Mock }` implementing all required interface methods via `m.Called(...)`. Use `.On(...)` expectations in tests.
   - Wrap server tests in a suite if there's shared server setup/teardown logic. If the tests are independent enough, plain functions with assert/require are acceptable (Claude's discretion per CONTEXT.md).
   - Convert all assertions to assert/require.

2. **p2p/relay_test.go** (394 lines) -- Suite + assertion migration:
   - **fullMockChainRepo**: KEEP hand-rolled -- has real in-memory storage with maps and mutex. Only migrate its test assertions.
   - **mockUTXORepo**: KEEP hand-rolled -- real in-memory UTXO store.
   - Wrap in a P2P relay suite if shared setup exists (server creation, peer connection helpers). Move repeated helper functions into suite methods.
   - Convert all `t.Errorf`/`t.Fatalf` to suite assert/require.

3. **p2p/sync_test.go** (291 lines) -- Suite + assertion migration:
   - Uses same fullMockChainRepo/mockUTXORepo fakes as relay_test (they're in the same package). Keep hand-rolled.
   - Wrap in suite if shared setup exists, or keep plain functions with assert/require.
   - Convert all assertions.

4. **p2p/reorg_test.go** (267 lines) -- Suite + assertion migration:
   - **reorgMockChainRepo**: KEEP hand-rolled -- extends fullMockChainRepo with undo storage.
   - Convert all assertions to assert/require.
   - Wrap in suite if shared setup exists.

5. After migrating all 4 files, run the full test suite to verify zero regressions:
   `go test ./...`

6. Final verification -- confirm no raw assertions remain anywhere:
   `grep -rn 't\.Errorf\|t\.Fatalf\|t\.Error(' internal/ --include='*_test.go'`
   This MUST return zero matches.
  </action>
  <verify>
    <automated>cd /Users/baotoq/Work/shitcoin && go test ./... && test $(grep -rn 't\.Errorf\|t\.Fatalf\|t\.Error(' internal/ --include='*_test.go' | wc -l) -eq 0</automated>
  </verify>
  <done>All 4 P2P test files migrated with suites where appropriate, server_test mockChainRepo uses testify/mock, all fakes preserved, full test suite passes, grep confirms zero remaining raw assertions in entire codebase.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `go test ./...` passes with zero failures.
2. `grep -rn 't\.Errorf\|t\.Fatalf\|t\.Error(' internal/ --include='*_test.go'` returns zero matches across ALL 19 test files.
3. `grep -rn 'suite\.Suite' internal/ --include='*_test.go'` shows suite usage in bbolt and P2P test files.
4. `grep -rn 'mock\.Mock' internal/ --include='*_test.go'` shows testify/mock in server_test.go and chain_test.go.
</verification>

<success_criteria>
- All 7 complex test files fully migrated
- bbolt repos use testify/suite with per-test DB setup
- P2P tests use testify/suite where shared setup exists
- server_test.go mockChainRepo converted to testify/mock
- chain_test.go mockMempoolAdder converted to testify/mock
- Hand-rolled fakes (fullMockChainRepo, mockUTXORepo, reorgMockChainRepo) preserved
- Zero raw t.Errorf/t.Fatalf/t.Error( in entire codebase
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-use-test-assert/04.1-02-SUMMARY.md`
</output>
