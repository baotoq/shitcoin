---
phase: 12-local-k8s-development
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Makefile
autonomous: true
requirements: [DEV-04]

must_haves:
  truths:
    - "make test runs Go tests"
    - "make lint runs golangci-lint"
    - "make ci runs full CI checks locally (test + lint + frontend)"
    - "make docker-build builds both Docker images"
    - "make tilt-up creates kind cluster and starts Tilt"
  artifacts:
    - path: "Makefile"
      provides: "Common development commands"
      contains: ".PHONY"
  key_links:
    - from: "Makefile"
      to: "deploy/k8s/kind-cluster.yaml"
      via: "kind-create target references config path"
      pattern: "kind create cluster.*kind-cluster.yaml"
---

<objective>
Create a Makefile with common development commands wrapping Go tests, linting, Docker builds, kind cluster management, and Tilt startup.

Purpose: Single entry point for all common dev operations, reducing cognitive load and ensuring consistent command usage.
Output: Makefile
</objective>

<execution_context>
@/Users/baotoq/.claude/get-shit-done/workflows/execute-plan.md
@/Users/baotoq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-local-k8s-development/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Makefile with dev commands</name>
  <files>Makefile</files>
  <action>
Create `Makefile` in project root with the following phony targets:

**Core targets (required by DEV-04):**
- `test`: Run `go test ./...`
- `lint`: Run `golangci-lint run`
- `ci`: Run test + lint + frontend checks (`cd web && npm run lint && npm run build`). This target depends on `test` and `lint` targets.
- `docker-build`: Build both Docker images (`docker build -t shitcoin-backend .` and `docker build -t shitcoin-frontend web/`)
- `tilt-up`: Depends on `kind-create`, then runs `tilt up`

**Supporting targets:**
- `kind-create`: Run `kind create cluster --config deploy/k8s/kind-cluster.yaml` with `|| true` for idempotency (per research pitfall 4)
- `kind-delete`: Run `kind delete cluster --name shitcoin`

All targets should be declared `.PHONY` since none produce files.

Use tabs for indentation (Makefile requirement). Add brief comments above each target explaining what it does.
  </action>
  <verify>
    <automated>test -f Makefile && make -n test 2>/dev/null && make -n lint 2>/dev/null && make -n ci 2>/dev/null && make -n docker-build 2>/dev/null && echo "PASS"</automated>
  </verify>
  <done>Makefile exists with all required targets (test, lint, ci, docker-build, tilt-up) and supporting targets (kind-create, kind-delete). All targets are .PHONY. `make -n <target>` succeeds for each target (dry-run validates syntax).</done>
</task>

</tasks>

<verification>
- `make -n test` shows `go test ./...`
- `make -n lint` shows `golangci-lint run`
- `make -n ci` invokes test, lint, and frontend checks
- `make -n docker-build` shows both docker build commands
- `make -n tilt-up` shows kind-create dependency then tilt up
- `make -n kind-create` shows kind create with config path
</verification>

<success_criteria>
- Makefile exists with correct syntax (tabs, .PHONY)
- All 7 targets work in dry-run mode (make -n)
- ci target composes test + lint + frontend checks
- tilt-up depends on kind-create for automatic cluster setup
- kind-create is idempotent (uses || true)
</success_criteria>

<output>
After completion, create `.planning/phases/12-local-k8s-development/12-02-SUMMARY.md`
</output>
