---
phase: 11-kubernetes-manifests
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - deploy/k8s/overlays/dev/kustomization.yaml
  - deploy/k8s/overlays/prod/kustomization.yaml
autonomous: true
requirements:
  - K8S-05
  - K8S-06

must_haves:
  truths:
    - "kubectl kustomize deploy/k8s/overlays/dev renders valid YAML with local image refs and lower resource limits"
    - "kubectl kustomize deploy/k8s/overlays/prod renders valid YAML with GHCR image refs and production resource limits"
    - "Both overlays render all 6 base resources without errors"
  artifacts:
    - path: "deploy/k8s/overlays/dev/kustomization.yaml"
      provides: "Dev overlay with local images (latest tag) and reduced resources"
      contains: "newTag: latest"
    - path: "deploy/k8s/overlays/prod/kustomization.yaml"
      provides: "Prod overlay with GHCR images (pinned SHA tag) and production resources"
      contains: "ghcr.io"
  key_links:
    - from: "deploy/k8s/overlays/dev/kustomization.yaml"
      to: "deploy/k8s/base"
      via: "resources: [../../base]"
      pattern: "../../base"
    - from: "deploy/k8s/overlays/prod/kustomization.yaml"
      to: "deploy/k8s/base"
      via: "resources: [../../base]"
      pattern: "../../base"
---

<objective>
Create Kustomize dev and prod overlays that customize the base manifests with environment-specific image references and resource limits.

Purpose: Dev overlay uses local image names with :latest tags and minimal resources for local K8s development. Prod overlay uses GHCR image references with pinned SHA tags and higher resource limits for production deployment.
Output: 2 overlay kustomization.yaml files under deploy/k8s/overlays/{dev,prod}/
</objective>

<execution_context>
@/Users/baotoq/.claude/get-shit-done/workflows/execute-plan.md
@/Users/baotoq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-kubernetes-manifests/11-RESEARCH.md
@.planning/phases/11-kubernetes-manifests/11-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: Base manifests that overlays customize -->

Base image names used in Deployments (set in base, overridden by overlays):
- Backend container: image "shitcoin-backend", container name "backend"
- Frontend container: image "shitcoin-frontend", container name "frontend"

Base resource limits (overridden by overlays):
- Backend: requests cpu=100m memory=128Mi, limits cpu=500m memory=256Mi
- Frontend: requests cpu=50m memory=64Mi, limits cpu=200m memory=128Mi

GHCR image names (from Phase 10 CI):
- Backend: ghcr.io/baotoq/shitcoin
- Frontend: ghcr.io/baotoq/shitcoin-web

Deployment names: "backend", "frontend"
Container names: "backend", "frontend"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dev overlay with local images and low resources</name>
  <files>deploy/k8s/overlays/dev/kustomization.yaml</files>
  <action>
Create directory deploy/k8s/overlays/dev/ and write kustomization.yaml:

- apiVersion: kustomize.config.k8s.io/v1beta1, kind: Kustomization
- resources: [../../base]
- images transformer:
  - name: shitcoin-backend, newName: shitcoin-backend, newTag: latest
  - name: shitcoin-frontend, newName: shitcoin-frontend, newTag: latest
- patches (strategic merge) to override resource limits:
  - Backend Deployment (target kind: Deployment, name: backend):
    - Container "backend": requests cpu=50m memory=64Mi, limits cpu=200m memory=128Mi
  - Frontend Deployment (target kind: Deployment, name: frontend):
    - Container "frontend": requests cpu=25m memory=32Mi, limits cpu=100m memory=64Mi

Use inline patch format with `target:` and `patch: |-` syntax as shown in the research.
  </action>
  <verify>
    <automated>kubectl kustomize deploy/k8s/overlays/dev 2>&1 | grep -c 'kind: Deployment' | grep 2 && kubectl kustomize deploy/k8s/overlays/dev 2>&1 | grep 'newTag\|:latest\|shitcoin-backend:latest\|image: shitcoin-backend' && echo "dev overlay OK"</automated>
  </verify>
  <done>Dev overlay renders valid YAML with local image:latest tags and reduced resource limits</done>
</task>

<task type="auto">
  <name>Task 2: Create prod overlay with GHCR images and production resources</name>
  <files>deploy/k8s/overlays/prod/kustomization.yaml</files>
  <action>
Create directory deploy/k8s/overlays/prod/ and write kustomization.yaml:

- apiVersion: kustomize.config.k8s.io/v1beta1, kind: Kustomization
- resources: [../../base]
- images transformer:
  - name: shitcoin-backend, newName: ghcr.io/baotoq/shitcoin, newTag: sha-abc1234 (placeholder pinned SHA)
  - name: shitcoin-frontend, newName: ghcr.io/baotoq/shitcoin-web, newTag: sha-abc1234 (placeholder pinned SHA)
- patches (strategic merge) to set production resource limits:
  - Backend Deployment (target kind: Deployment, name: backend):
    - Container "backend": requests cpu=200m memory=256Mi, limits cpu=1 memory=512Mi
  - Frontend Deployment (target kind: Deployment, name: frontend):
    - Container "frontend": requests cpu=50m memory=64Mi, limits cpu=200m memory=128Mi

After creating, run full validation:
- `kubectl kustomize deploy/k8s/overlays/prod` must render valid YAML
- Output must contain ghcr.io image references
- `kubectl apply -k deploy/k8s/overlays/dev --dry-run=client` must pass
- `kubectl apply -k deploy/k8s/overlays/prod --dry-run=client` must pass
  </action>
  <verify>
    <automated>kubectl kustomize deploy/k8s/overlays/prod 2>&1 | grep 'ghcr.io' && kubectl apply -k deploy/k8s/overlays/dev --dry-run=client 2>&1 | tail -5 && kubectl apply -k deploy/k8s/overlays/prod --dry-run=client 2>&1 | tail -5 && echo "both overlays valid"</automated>
  </verify>
  <done>Prod overlay renders valid YAML with ghcr.io/baotoq/shitcoin images and production resource limits. Both dev and prod overlays pass dry-run validation.</done>
</task>

</tasks>

<verification>
Both overlays render valid YAML and pass `kubectl apply --dry-run=client` validation.
Dev overlay uses local image:latest with lower resources.
Prod overlay uses ghcr.io images with pinned tags and higher resources.
</verification>

<success_criteria>
- `kubectl kustomize deploy/k8s/overlays/dev` renders with shitcoin-backend:latest and shitcoin-frontend:latest
- `kubectl kustomize deploy/k8s/overlays/prod` renders with ghcr.io/baotoq/shitcoin:sha-abc1234 images
- `kubectl apply -k deploy/k8s/overlays/dev --dry-run=client` succeeds
- `kubectl apply -k deploy/k8s/overlays/prod --dry-run=client` succeeds
- Dev resources are lower than base; prod resources are higher than base
</success_criteria>

<output>
After completion, create `.planning/phases/11-kubernetes-manifests/11-02-SUMMARY.md`
</output>
