---
phase: 05.1-upgrade-to-go-1-26-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - internal/infrastructure/persistence/jsonfile/wallet_repo_test.go
  - internal/domain/block/merkle.go
  - internal/domain/wallet/base58.go
autonomous: true
requirements:
  - UPGRADE-01
  - UPGRADE-02
  - UPGRADE-03
  - UPGRADE-04

must_haves:
  truths:
    - "go.mod declares go 1.26.1"
    - "All existing tests pass on Go 1.26.1"
    - "Code compiles cleanly with go build"
    - "go vet reports no issues"
    - "go fix modernizers applied (interface{} -> any, rangeint)"
  artifacts:
    - path: "go.mod"
      provides: "Go version directive"
      contains: "go 1.26.1"
    - path: "go.sum"
      provides: "Updated dependency checksums"
  key_links:
    - from: "go.mod"
      to: "all packages"
      via: "go directive controls language version"
      pattern: "go 1\\.26\\.1"
---

<objective>
Upgrade the project from Go 1.24.0 to Go 1.26.1, applying automatic code modernizations via `go fix` and verifying all tests pass.

Purpose: Keep the project on a current Go version with security patches and benefit from language improvements (Green Tea GC, range-over-int, any alias).
Output: Updated go.mod/go.sum, modernized code via go fix, all tests green.
</objective>

<execution_context>
@/Users/baotoq/.claude/get-shit-done/workflows/execute-plan.md
@/Users/baotoq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-upgrade-to-go-1-26-1/05.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Go version and tidy dependencies</name>
  <files>go.mod, go.sum</files>
  <action>
1. Edit go.mod to change `go 1.24.0` to `go 1.26.1` (the go directive line only, do not add a toolchain directive).
2. Run `go mod tidy` to update dependencies and checksums for Go 1.26.1.
3. Run `go build ./...` to verify the project compiles cleanly.
4. Run `go vet ./...` to verify no vet warnings.

The Go toolchain will automatically download Go 1.26.1 when it sees the updated directive (current install is via Go toolchain mechanism at ~/go/pkg/mod/golang.org/toolchain@...).

Do NOT manually update any dependency versions -- let `go mod tidy` handle minimum version selection.
  </action>
  <verify>
    <automated>grep 'go 1.26.1' go.mod && go build ./... && go vet ./...</automated>
  </verify>
  <done>go.mod declares go 1.26.1, project builds and vets cleanly, go.sum updated</done>
</task>

<task type="auto">
  <name>Task 2: Apply go fix modernizers and verify tests</name>
  <files>internal/infrastructure/persistence/jsonfile/wallet_repo_test.go, internal/domain/block/merkle.go, internal/domain/wallet/base58.go</files>
  <action>
1. Run `go fix ./...` to apply all relevant modernizers automatically. Expected transformations per research:
   - `interface{}` -> `any` (wallet_repo_test.go)
   - `for i := 0; i < N; i++` -> `for i := range N` where safe (merkle.go, base58.go, test files)
   - `min()`/`max()` builtins where applicable
2. Review the diff of `go fix` changes. Verify no semantic changes -- particularly that rangeint transforms did NOT apply to loops where the loop variable is modified in the body or the upper bound changes during iteration (e.g., `len(level)` in merkle.go's Merkle tree computation).
3. Run `go test ./...` to verify all tests pass.
4. Run `go test -race ./...` to verify no race conditions.

The files_modified list above is based on research analysis. `go fix` may touch additional files -- that is expected. All changes are mechanical AST transforms.

If any test fails, investigate whether it reveals a real bug exposed by Go 1.25's nil-pointer behavior fix (see research Pitfall 1). Fix the actual bug rather than reverting the Go upgrade.
  </action>
  <verify>
    <automated>go test ./... && go test -race ./...</automated>
  </verify>
  <done>All go fix modernizers applied, all tests pass including race detector, no regressions</done>
</task>

</tasks>

<verification>
1. `grep 'go 1.26.1' go.mod` confirms version directive
2. `go build ./...` compiles without errors
3. `go vet ./...` reports no issues
4. `go test ./...` all tests pass
5. `go test -race ./...` no race conditions detected
</verification>

<success_criteria>
- go.mod declares `go 1.26.1`
- All existing tests pass on Go 1.26.1
- go fix modernizers applied (interface{} -> any, rangeint where safe)
- go vet clean
- No regressions from the upgrade
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-upgrade-to-go-1-26-1/05.1-01-SUMMARY.md`
</output>
