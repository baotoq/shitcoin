---
phase: 15-domain-layer-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/domain/tx/transaction_test.go
  - internal/domain/utxo/set_test.go
  - internal/domain/wallet/wallet_test.go
  - internal/domain/wallet/base58_test.go
  - internal/domain/mempool/mempool_test.go
autonomous: true
requirements: [DOM-03, DOM-04]

must_haves:
  truths:
    - "tx package coverage reaches 95%+"
    - "utxo package coverage reaches 95%+"
    - "wallet package coverage reaches 95%+"
    - "mempool package coverage reaches 95%+"
    - "Error path tests exist for invalid signatures, corrupt UTXO data, and boundary conditions"
  artifacts:
    - path: "internal/domain/tx/transaction_test.go"
      provides: "tx error path tests (SignTransaction coinbase, VerifyTransaction invalid sig/key, ValidateCoinbase multi-output)"
      contains: "TestSignTransaction_Coinbase"
    - path: "internal/domain/utxo/set_test.go"
      provides: "UndoBlock error paths, ApplyBlock repo errors, GetBalance repo errors"
      contains: "TestUndoBlock_Error"
    - path: "internal/domain/wallet/wallet_test.go"
      provides: "PubKeyHashFromAddress tests (valid, invalid checksum, wrong version, wrong length)"
      contains: "TestPubKeyHashFromAddress"
    - path: "internal/domain/wallet/base58_test.go"
      provides: "Base58CheckDecode short input edge case"
      contains: "short"
    - path: "internal/domain/mempool/mempool_test.go"
      provides: "GetByID found/not-found, FeeForTx not-found, Remove non-existent"
      contains: "TestGetByID"
  key_links:
    - from: "internal/domain/utxo/set_test.go"
      to: "internal/testutil/mock_utxo_repo.go"
      via: "mock repo returning errors"
      pattern: "NewMockUTXORepo"
    - from: "internal/domain/wallet/wallet_test.go"
      to: "internal/domain/wallet/address.go"
      via: "PubKeyHashFromAddress function"
      pattern: "PubKeyHashFromAddress"
---

<objective>
Gap-fill tx, utxo, wallet, and mempool packages from current coverage (86-94%) to 95%+ each.

Purpose: These four packages are closest to the 95% target and need only targeted tests for specific uncovered functions and error branches identified in research.
Output: Extended test files with error path coverage for all four packages.
</objective>

<execution_context>
@/Users/baotoq/.claude/get-shit-done/workflows/execute-plan.md
@/Users/baotoq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-domain-layer-coverage/15-RESEARCH.md

@internal/domain/tx/transaction_test.go
@internal/domain/tx/transaction.go
@internal/domain/utxo/set_test.go
@internal/domain/utxo/set.go
@internal/domain/wallet/wallet_test.go
@internal/domain/wallet/wallet.go
@internal/domain/wallet/address.go
@internal/domain/wallet/base58_test.go
@internal/domain/wallet/base58.go
@internal/domain/mempool/mempool_test.go
@internal/domain/mempool/mempool.go
@internal/testutil/builders.go
@internal/testutil/mock_utxo_repo.go
@internal/testutil/mock_wallet_repo.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gap-fill tx and mempool test coverage to 95%+</name>
  <files>internal/domain/tx/transaction_test.go, internal/domain/mempool/mempool_test.go</files>
  <action>
**tx package** (94.4% -> 95%+): Add these tests to transaction_test.go:
1. `TestSignTransaction_CoinbaseNoop` -- Call SignTransaction on a coinbase tx, verify it returns nil and does not modify the transaction. The coinbase early-return branch is currently only implicitly covered.
2. `TestVerifyTransaction_InvalidSignatures` -- Table-driven test with cases: empty signature bytes, empty pubkey bytes, invalid (non-parseable) signature bytes, invalid (non-parseable) pubkey bytes. Each should return false. Use the tx package's internal test style (package tx, not tx_test) to construct Transaction structs with tampered fields.
3. `TestValidateCoinbase_MultipleOutputs` -- Construct a coinbase transaction with 2+ outputs, verify ValidateCoinbase returns ErrInvalidCoinbase. May need to use internal package access to construct a multi-output coinbase.

**mempool package** (90.9% -> 95%+): Add these tests to mempool_test.go:
1. `TestGetByID_Found` -- Add a transaction to mempool, call GetByID with its hash, verify the returned transaction matches.
2. `TestGetByID_NotFound` -- Call GetByID with a hash not in the mempool, verify nil is returned.
3. `TestRemove_NonExistentTxID` -- Call Remove with a tx ID that's not in the mempool, verify no panic and mempool state is unchanged.
4. `TestFeeForTx_NotFound` -- Call FeeForTx with a hash not in the mempool, verify it returns 0.

Follow existing test patterns: use testutil builders where possible, testify require/assert, table-driven where multiple cases exist. For mempool tests, use existing `buildSignedTx` helper pattern already in mempool_test.go.
  </action>
  <verify>
    <automated>cd /Users/baotoq/Work/shitcoin && go test -cover ./internal/domain/tx/ ./internal/domain/mempool/ -count=1</automated>
  </verify>
  <done>tx package coverage >= 95%, mempool package coverage >= 95%, all new tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Gap-fill utxo and wallet test coverage to 95%+</name>
  <files>internal/domain/utxo/set_test.go, internal/domain/wallet/wallet_test.go, internal/domain/wallet/base58_test.go</files>
  <action>
**utxo package** (86.2% -> 95%+): Add these tests to set_test.go:
1. `TestUndoBlock_ErrorPaths` -- Table-driven test covering:
   - Invalid created txID hex in UndoEntry (should error on hex decode)
   - Repo Delete error during undo (configure mock to return error)
   - Invalid spent txID hex in UndoEntry (should error on hex decode)
   - Repo Put error during undo (configure mock to return error)
   Use testutil.NewMockUTXORepo() and configure it to return errors for specific operations.
2. `TestApplyBlock_RepoErrors` -- Test ApplyBlock when:
   - repo.Put returns an error (during output creation)
   - repo.Delete returns an error (during input spending)
   Configure MockUTXORepo to fail on specific calls.
3. `TestGetBalance_RepoError` -- Test GetBalance when repo.GetByAddress returns an error, verify the error propagates.

**wallet package** (87.6% -> 95%+): Add these tests:
1. `TestPubKeyHashFromAddress` in wallet_test.go -- Table-driven test covering:
   - Valid address (create a wallet, get its address, extract pubkey hash, verify it matches)
   - Invalid checksum (tamper with address bytes)
   - Invalid Base58 characters
   - Address too short
   Use external test package (package wallet_test) consistent with existing wallet tests.
2. `TestBase58CheckDecode_ShortInput` in base58_test.go -- Test with a very short Base58 string that decodes to fewer than 5 bytes, verify error is returned.

Follow existing patterns: external test packages for wallet, testutil mock repos for UTXO, testify assertions.
  </action>
  <verify>
    <automated>cd /Users/baotoq/Work/shitcoin && go test -cover ./internal/domain/utxo/ ./internal/domain/wallet/ -count=1</automated>
  </verify>
  <done>utxo package coverage >= 95%, wallet package coverage >= 93% (accounting for unreachable NewWallet crypto error branch per research open question), all new tests pass</done>
</task>

</tasks>

<verification>
```bash
# Verify all four packages meet coverage thresholds
go test -cover ./internal/domain/tx/ ./internal/domain/utxo/ ./internal/domain/wallet/ ./internal/domain/mempool/ -count=1

# Verify error path tests exist by name
go test -v ./internal/domain/... -run "Error|Invalid|Reject|Fail|Nil|Corrupt|Boundary|NotFound|Noop|ShortInput" -count=1
```
</verification>

<success_criteria>
- tx package: 95%+ coverage
- mempool package: 95%+ coverage
- utxo package: 95%+ coverage
- wallet package: 93%+ coverage (95% target minus unreachable crypto branch)
- All tests pass with -count=2 (no shared state)
- Error path test names clearly identify what error condition they exercise
</success_criteria>

<output>
After completion, create `.planning/phases/15-domain-layer-coverage/15-01-SUMMARY.md`
</output>
