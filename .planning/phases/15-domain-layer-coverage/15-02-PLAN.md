---
phase: 15-domain-layer-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/domain/chain/chain_test.go
autonomous: true
requirements: [DOM-01, DOM-04]

must_haves:
  truths:
    - "Chain aggregate coverage reaches 85%+"
    - "Difficulty adjustment logic is tested at interval boundaries"
    - "MineBlock error paths are tested (nil latestBlock, repo save error, mining progress callback)"
    - "SetLatestBlock and Initialize error paths are covered"
  artifacts:
    - path: "internal/domain/chain/chain_test.go"
      provides: "Tests for getCurrentBits, SetLatestBlock, Initialize errors, MineBlock errors, Reorganize edge cases"
      contains: "TestGetCurrentBits"
  key_links:
    - from: "internal/domain/chain/chain_test.go"
      to: "internal/testutil/mock_chain_repo.go"
      via: "mock repo for error injection"
      pattern: "NewMockChainRepo"
    - from: "internal/domain/chain/chain_test.go"
      to: "internal/domain/chain/chain.go"
      via: "testing getCurrentBits via MineBlock at adjustment interval"
      pattern: "DifficultyAdjustInterval"
---

<objective>
Bring chain aggregate test coverage from 69.5% to 85%+ by testing difficulty adjustment, SetLatestBlock, and error paths in Initialize, MineBlock, and Reorganize.

Purpose: The chain aggregate is the core orchestration layer. Coverage gaps exist in difficulty adjustment (18.8%), SetLatestBlock (0%), and several error branches in mining and reorg logic.
Output: Extended chain_test.go with ~10-12 new test functions.
</objective>

<execution_context>
@/Users/baotoq/.claude/get-shit-done/workflows/execute-plan.md
@/Users/baotoq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-domain-layer-coverage/15-RESEARCH.md

@internal/domain/chain/chain.go
@internal/domain/chain/chain_test.go
@internal/testutil/mock_chain_repo.go
@internal/testutil/mock_utxo_repo.go
@internal/testutil/builders.go
@internal/domain/block/block.go
@internal/domain/block/pow.go
@internal/domain/utxo/set.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test difficulty adjustment and SetLatestBlock</name>
  <files>internal/domain/chain/chain_test.go</files>
  <action>
Add these tests to chain_test.go (package chain_test, external test package):

1. **TestGetCurrentBits_AdjustmentInterval** -- Test difficulty adjustment by mining exactly DifficultyAdjustInterval blocks:
   - Create chain with `DifficultyAdjustInterval: 5, BlockTimeTarget: 600, InitialDifficulty: 1`
   - Initialize and mine 5 blocks (to trigger adjustment at block 5)
   - Verify chain reaches height 5 without error (difficulty adjustment ran)
   - Mine one more block to confirm adjusted difficulty is used
   - This exercises the `getCurrentBits` function's adjustment branch

2. **TestGetCurrentBits_BeforeInterval** -- Verify that blocks before the adjustment interval use initial difficulty:
   - Create chain with `DifficultyAdjustInterval: 10, InitialDifficulty: 1`
   - Mine 3 blocks (well before interval)
   - Verify all mine successfully with initial difficulty

3. **TestSetLatestBlock** -- Simple test:
   - Create chain, initialize
   - Call SetLatestBlock with a different block
   - Verify LatestBlock() returns the new block
   - Verify Height() returns the new block's height

4. **TestInitialize_AlreadyInitialized** -- Test Initialize when repo already has blocks:
   - Pre-seed MockChainRepo with a genesis block (set LatestBlock in repo)
   - Call Initialize, verify it loads existing chain instead of creating new genesis

5. **TestInitialize_EmptyMinerAddress** -- Test Initialize with empty miner address when no existing chain:
   - Call Initialize(ctx, "") with empty repo
   - Verify behavior (error or genesis with empty coinbase -- check source to determine expected behavior)

Follow existing test patterns in chain_test.go: use testutil.NewMockChainRepo(), testutil.NewMockUTXORepo(), chain.ChainConfig with InitialDifficulty: 1.
  </action>
  <verify>
    <automated>cd /Users/baotoq/Work/shitcoin && go test -cover ./internal/domain/chain/ -count=1</automated>
  </verify>
  <done>Tests for difficulty adjustment, SetLatestBlock, and Initialize paths pass. Coverage trending toward 85%.</done>
</task>

<task type="auto">
  <name>Task 2: Test MineBlock and Reorganize error paths</name>
  <files>internal/domain/chain/chain_test.go</files>
  <action>
Add these tests to chain_test.go:

1. **TestMineBlock_BeforeInitialize** -- Call MineBlock on an uninitialized chain (latestBlock is nil), verify it returns an error.

2. **TestMineBlock_WithTransactions** -- Mine a block that includes user transactions (not just coinbase):
   - Initialize chain, mine one block to get spendable UTXO
   - Create a signed transaction spending the coinbase output
   - Call MineBlock with the transaction list
   - Verify the mined block contains both coinbase and user tx

3. **TestMineBlock_WithMiningProgress** -- Test the OnMiningProgress callback path:
   - Set chain's OnMiningProgress callback to a function that records calls
   - Mine a block
   - Verify the callback was invoked (exercises the MineWithProgress code path)

4. **TestMineBlock_SaveBlockWithUTXOsError** -- Test when repo.SaveBlockWithUTXOs returns an error:
   - Configure MockChainRepo to return an error on SaveBlockWithUTXOs
   - Call MineBlock, verify the error propagates

5. **TestReorganize_EmptyForkBlocks** -- Call Reorganize with an empty fork blocks slice, verify behavior (should handle gracefully).

6. **TestReorganize_InvalidForkBlock** -- Call Reorganize with fork blocks that have invalid PoW, verify it returns an error (the ValidatePoW check in Reorganize).

After adding all tests, run coverage. If still below 85%, examine the coverage profile to identify remaining gaps:
```bash
go test -coverprofile=chain_cover.out ./internal/domain/chain/
go tool cover -func=chain_cover.out
```
Add additional targeted tests for any remaining uncovered functions until 85% is reached.
  </action>
  <verify>
    <automated>cd /Users/baotoq/Work/shitcoin && go test -cover ./internal/domain/chain/ -count=1 | grep -E "coverage: [0-9]"</automated>
  </verify>
  <done>Chain package coverage >= 85%, all error path tests pass, MineBlock and Reorganize edge cases covered</done>
</task>

</tasks>

<verification>
```bash
# Verify chain coverage meets 85% threshold
go test -cover ./internal/domain/chain/ -count=1

# Verify per-function coverage
go test -coverprofile=/tmp/chain_cover.out ./internal/domain/chain/ && go tool cover -func=/tmp/chain_cover.out

# Verify error path tests exist
go test -v ./internal/domain/chain/ -run "Error|Invalid|Before|Empty|Progress" -count=1
```
</verification>

<success_criteria>
- Chain package: 85%+ coverage
- getCurrentBits tested at adjustment interval boundary
- SetLatestBlock tested (was 0% coverage)
- MineBlock error paths tested (nil latest, save error, progress callback)
- Reorganize edge cases tested (empty fork, invalid PoW)
- All tests pass with -count=2 (no shared state)
</success_criteria>

<output>
After completion, create `.planning/phases/15-domain-layer-coverage/15-02-SUMMARY.md`
</output>
